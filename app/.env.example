# Copy to .env.local to configure the app.

# "true" (default) serves the in-memory mock data layer.
# Set to "false" once the Spring Boot backend is available.
VITE_USE_MOCKS=true

# Base URL of the backend API (used only when VITE_USE_MOCKS=false).
VITE_API_URL=http://localhost:8080/api/v1
